git add . para meter las cosas y prepararlas para el envio

git commit -m "Corregir colores de interfaz" esto es la etiqueta que se le da a la caja

git push envia todo al git 

git add .

git commit -m "Corregir colores de interfaz"

git push